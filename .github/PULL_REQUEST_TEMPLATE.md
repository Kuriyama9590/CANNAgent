## 变更说明

（做了什么、为什么；涉及接口变更时说明兼容性）

## 关联 issue

Closes #（写 `Closes #编号` 可在合并时自动关闭；部分完成用 `Refs #编号`）

## 自检清单

- [ ] 提交信息符合 Conventional Commits（CI 会校验）
- [ ] CI 全绿（web-demo 构建 / 提交信息规范）
- [ ] 涉及行为变更时已更新 docs/ 对应规范
- [ ] 不包含密钥 / 敏感信息（凭据只走 `.env`，见 `.env.example`）
